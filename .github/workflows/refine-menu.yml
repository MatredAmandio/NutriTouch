name: NutriTouch legacy menu workflow disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node --check menu7.js
      - run: echo "Automatic source rewriting is disabled."
